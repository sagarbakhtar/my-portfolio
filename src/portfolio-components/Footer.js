import React from 'react';
import Navbar from 'react-bootstrap/Navbar';

function Footer(props) {
    return (
    <Navbar variant="dark" bg="primary" expand="lg" style={{height: '20px', marginTop: '10px'}}>
    </Navbar>
    );
}

export { Footer };